'use strict';


// reference another file like this
// require('./utilities');
// then we can just use the variables that it defines


describe("some test", function() {
it('does basic stuff', function () {
expect(1).toEqual(1);
});
it('does right stuff', function() {
expect(1).toEqual(1);
});
it('does other correct stuff', function() {
expect(1).toEqual(1);
});
it('does other right stuff', function() {
expect(1).toEqual(1);
});
});
